services:
  postgres:
    image: postgres:15.15
    ports:
      - "55432:5432"
    environment:
      - POSTGRES_USER=docker
      - POSTGRES_PASSWORD=docker
      - POSTGRES_DB=polls
    volumes:
      - polls_pg_data:/postgresql
      
volumes:
  polls_pg_data:
